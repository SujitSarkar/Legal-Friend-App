name: legal_friend
description: A new Flutter project.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1
environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  provider:
  aamarpay:
  flutter_easyloading:
  intl:
  shared_preferences:
  cloud_firestore:
  flutter_spinkit:
#  flutter_launcher_icons: ^0.9.0

dev_dependencies:
  flutter_test:
    sdk: flutter
flutter_icons:
    android: "launcher_icon"
    ios: true
    image_path: "assets/logo/logo_wbg.png"

# The following section is specific to Flutter.
flutter:
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  assets:
    - assets/bd-districts.json
    - assets/logo/logo_trans.png
    - assets/logo/logo_wbg.png
    - assets/logo/splash_image.png
    #Home Image
    - assets/home_image/bodli_khana.png
    - assets/home_image/kaj_list.png


  fonts:
    - family: niladriFontLite
      fonts:
        - asset: assets/fonts/li_Niladri_Nodia_ANSI_V1.ttf
  #        style: italic

    - family: barkatFontBold
      fonts:
        - asset: assets/fonts/li_Shohid_Barkat_ANSI_V2.ttf
#          weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/custom-fonts/#from-packages
